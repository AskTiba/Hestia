services:
  - type: web
    name: hestia-backend
    env: node
    rootDir: backend
    buildCommand: npm install -g bun && bun install && bun run build
    startCommand: bun run start:prod
    envVars:
      - key: DATABASE_URL
        sync: false
